# Respaldos JSON

Guarda aqui los archivos `.json` exportados desde el levantamiento o desde el certificador.

Si cargas un JSON en la aplicacion, los registros nuevos se agregan sin eliminar los anteriores.
